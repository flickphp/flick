<div class="field">
    <div class="control">
        <button type="submit" id="{{ id }}" class="button is-primary {{ classes }}" {{attributes}}>
            {{ label }}
        </button>
    </div>
</div>
